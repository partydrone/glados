<h1>Enrollment Confirmation</h1>


<p>Training Event</p>

<%= @training_event.title %>
<%= @training_event.description %>
<%= @training_event.location %>
Starts <%= l @training_event.started_at.to_date, format: :long %>
Ends <%= l @training_event.ended_at.to_date, format: :long %>

<p>Courses you registered for</p>
<ul>
   <% @training_courses.each do |course| %>
   <li><%= course.number %> - <%= course.title %></li>
   <%end%>
</ul>
<p>To respond to this request, just reply to this email.</p>

