<% cache product_type do %>
  <%= content_tag_for :tr, product_type do %>
    <td class="handle admin-table-move">
      <%= image_tag 'icons/ellipsisup-small.svg', class: 'small-icon' %>
    </td>
    <td class="admin-left-edit">
      <%= render partial: 'options_edit', locals: {object: product_type, instance_name: product_type.name} %>
    </td>
    <td>
      <%= attachment_image_tag product_type, :icon_image, fallback: 'http://placehold.it/20/eaa800/ffffff', style: 'height: 1em;' %>
    </td>
    <td>
      <%= product_type.name %>
    </td>
    <td>
      <% product_type.name_backend.locales.each do |l| %>
        <%= content_tag :span, class: 'secondary label' do %>
          <%= humanize_locale(l) %>
          <% Mobility.with_locale(l) do %>
          <%= link_to 'x', admin_product_type_path(product_type, translation_locale: l), method: :delete, data: { confirm: %(Are you sure you want to delete this #{humanize_locale l} translation? This cannot be undone.) } %>
          <% end %>
        <% end %>
      <% end %>
    </td>
    <td class="admin-right-delete">
      <%= render partial: 'options_delete', locals: {object: product_type, instance_name: product_type.name} %>
    </td>
  <% end %>
<% end %>
