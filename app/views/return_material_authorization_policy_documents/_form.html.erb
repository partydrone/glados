<%= form_for @return_material_authorization_policy_document do |f| %>
  <%= content_tag :div, class: 'alert-box radius alert' do %>
    <strong><%= pluralize @return_material_authorization_policy_document.errors.count, 'error' %> kept this form from submitting:</strong>
    <ul>
      <% @return_material_authorization_policy_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end if @return_material_authorization_policy_document.errors.any? %>
  <%= f.label :title %>
  <%= f.text_field :title, value: 'Wavetronix Return Material Authorization Policy' %>
  <%= f.label :effective_on %>
  <%= f.date_field :effective_on, placeholder: 'yyyy-mm-dd' %>
  <%= f.label :body %>
  <%= f.text_area :body, rows: 20 %>
  <%= f.submit nil, class: 'button radius' %>
<% end %>
